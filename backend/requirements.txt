# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.29
alembic==1.13.1
aiosqlite==0.20.0   # SQLite async — locale
asyncpg==0.30.0     # PostgreSQL async — produzione (Supabase)

# Validation & Serialization
pydantic==2.11.1        # 2.11+ ha wheel cp314 per Python 3.14
pydantic-settings==2.7.1

# Auth
PyJWT==2.9.0           # python-jose sostituito: no Rust/cryptography richiesto per HS256
passlib[bcrypt]==1.7.4
bcrypt==4.0.1          # passlib 1.7.4 non compatibile con bcrypt>=4.1
email-validator==2.1.1 # richiesto da pydantic[email] per EmailStr

# Google Ads API (opzionale — solo per publish live, non per generazione/export)
# v29+ supporta Python 3.13+
google-ads==29.1.0

# HTTP client (for translation providers and website scraping)
httpx==0.27.0

# AI — auto-fill brief from hotel website
anthropic>=0.40.0

# Logging
structlog==24.1.0

# Dev / Test
pytest==8.1.1
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# Env
python-dotenv==1.0.1
